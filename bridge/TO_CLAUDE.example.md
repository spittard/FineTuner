# Handoff to Claude Code (replace body; save as `TO_CLAUDE.md`)

Repo root: `e:\projects\FineTuner\FineTuner`

## Your job

1. `cd` to repo root.
2. Read **`docs/CLAUDE_PLUGGING_CLOSED_LOOP.md`** and execute the phases that apply (RPC must be up for rematch / regression tests).
3. Report: commands run, exit codes, paths to artifacts.

## Context from Cursor (edit below)

- …

## Do not

Ask the human to run the long pipeline unless RPC is unavailable—run it yourself when possible.
